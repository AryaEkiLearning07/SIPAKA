# --- Database ---
# Dev lokal: XAMPP MariaDB (port 3307 pada mesin AryaEki07; XAMPP bawaan umumnya 3306)
DATABASE_URL="mysql://root@localhost:3307/lexvera_db"
# Produksi (rencana): PostgreSQL 16 + pgvector
# DATABASE_URL="postgresql://user:pass@host:5432/lexvera_db?schema=public"

# --- API ---
PORT=4000
HOST=0.0.0.0
# Matikan fallback demo di produksi (API wajib DB)
DEMO_FALLBACK="true"

# --- Web (Next.js) ---
NEXT_PUBLIC_API_URL="http://localhost:4000"
